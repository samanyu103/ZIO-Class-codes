#include<iostream>
using namespace std;
#define rep(i,n) for(int i=0; i<n;i++)
//earlier you used to declare and define at the same time. this must be done before the function is callled in main
// now you can declare before and define later on

int f(int a, int b);//declaration

int main() {
    f(0,3);
}
// definition
int f(int a, int b) {

}